import 'package:flutter/material.dart';

class FollowedStreamPage extends StatelessWidget {
  const FollowedStreamPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('FollowedStreamPage'),
      ),
    );
  }
}
