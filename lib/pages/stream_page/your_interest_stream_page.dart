import 'package:flutter/material.dart';

class InterestStreamPage extends StatelessWidget {
  const InterestStreamPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('InterestStreamPage'),
      ),
    );
  }
}
